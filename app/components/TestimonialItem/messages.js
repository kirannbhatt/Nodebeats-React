/*
 * TestimonialItem Messages
 *
 * This contains all the text for the TestimonialItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TestimonialItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TestimonialItem component!',
  },
});
