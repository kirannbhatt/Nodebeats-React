/*
 * NewsCategoryForm Messages
 *
 * This contains all the text for the NewsCategoryForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NewsCategoryForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NewsCategoryForm container!',
  },
});
