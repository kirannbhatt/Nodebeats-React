/*
 * TestimonialForm Messages
 *
 * This contains all the text for the TestimonialForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TestimonialForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TestimonialForm container!',
  },
});
