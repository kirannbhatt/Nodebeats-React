/*
 * Newss Messages
 *
 * This contains all the text for the Newss container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Newss';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Newss container!',
  },
});
