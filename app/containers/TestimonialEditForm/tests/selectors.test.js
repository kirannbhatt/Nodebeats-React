// import { fromJS } from 'immutable';
// import { selectTestimonialEditFormDomain } from '../selectors';

describe('selectTestimonialEditFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
