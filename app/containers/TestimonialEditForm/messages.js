/*
 * TestimonialEditForm Messages
 *
 * This contains all the text for the TestimonialEditForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TestimonialEditForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TestimonialEditForm container!',
  },
});
